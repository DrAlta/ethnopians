plant_row_02 /*(Coord Int -- Succes/Failure) plants seed  at Int multiples of  [x:5,Y:0] from coord  */= forth{
    dup
    lit(4)
    lt
    if
        swap
        lit(x: 5, y: 0)
        add
        dup
        plant_seed
        lit(Success)
        eq
        if
            swap
            lit(1)
            sub
            jump(plant_row_02)
        then
        lit(Failure)
        return
    then
    drop
    lit(Success)
    return
}