#[derive(Debug, Clone, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Tag {
    Beef,
    CrispyA, // Potential crunch
    Wet,     // Adds moisture
    Encapsulate, // The "shield" (e.g., a bun)
    FastFood,
    Meat,
    Citrus
}
